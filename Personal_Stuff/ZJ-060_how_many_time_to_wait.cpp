#include <iostream>

using namespace std;

int main() {
    int m;
    cin >> m;
    switch (m) {
        case (0 < m || m < 25) :
            cout << 25 - m << endl;
            break;
        case (25 < m || ) :
    }
    return 0;
}