#include <iostream>
using namespace std;

int main() {
    int n, a, b, c, d, e;
    cin >> n >> a >> b >> c >> d;
    for (size_t i = 0; i <= n; i++)
    {
        if () {
            
        }
    }
    
    return 0;
}